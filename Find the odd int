public class FindOdd {
    public static int findIt(int[] a) {
        int result = 0;
        for (int num : a) {
            result ^= num; // track number with an odd count
        }
        return result; // the remaining number appears an odd number of times.
    }
}
